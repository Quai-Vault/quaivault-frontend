server {
    listen 80;
    server_name testnet.quaivault.org app.quaivault.org;
    root /var/www/quai-vault/frontend/dist;
    index index.html;

    # Compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SPA routing - serve index.html for all routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: API proxy if needed
    # location /api {
    #     proxy_pass http://localhost:3001;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection 'upgrade';
    #     proxy_set_header Host $host;
    #     proxy_cache_bypass $http_upgrade;
    # }
}

# Redirect HTTP to HTTPS (uncomment when SSL is configured)
# server {
#     listen 80;
#     server_name testnet.quaivault.org app.quaivault.org;
#     return 301 https://$server_name$request_uri;
# }

# SSL Configuration (uncomment and configure when SSL certificates are ready)
# server {
#     listen 443 ssl http2;
#     server_name testnet.quaivault.org app.quaivault.org;
#     root /var/www/quai-vault/frontend/dist;
#     index index.html;
#
#     ssl_certificate /etc/letsencrypt/live/quaivault.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/quaivault.org/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # ... rest of configuration same as above
# }
